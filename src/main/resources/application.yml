
service:
  error:
    DEMO1: this is from DEMO1 %s    
    getnote: "%s"
    getallnotes: "%s"
    
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2
      
server:
  port: 8686
  
  